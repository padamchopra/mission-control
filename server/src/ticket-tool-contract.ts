export const REMY_TOOL_INSTRUCTIONS = `Use Remy as the source of truth for the user's workspaces, threads, agents, and tickets. A project or repository the user wants registered is a Remy workspace. When the user names a ticket key, read it before planning or changing code; a linked thread may omit the key. Use the thread tools when the user asks you to delegate, start another thread, continue one, or inspect its result. Keep product scope, findings, and status on the ticket. Do not claim Remy changed unless a tool confirms it.`;
